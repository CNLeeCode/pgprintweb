# Electron / electron-builder 二进制下载镜像
#
# 慢点根因：默认从 github.com/electron/electron/releases 下载
#   - electron-v22.3.27-win32-x64.zip (~97 MB)
#   - winCodeSign / nsis / nsis-resources (~30 MB)
# GitHub Actions runner（美国）→ github.com 走外网，网速不稳，最差几分钟下不完。
#
# 解法：切到 npmmirror 镜像（原淘宝镜像），CDN 稳定，首次下载也能跑满带宽。
#
# 为什么用 .npmrc 而非只靠环境变量：
#   @electron/get 在 electron-builder 24.x 内部 spawn 子进程下载时，
#   环境变量（ELECTRON_MIRROR）偶尔因继承问题丢失，.npmrc 是 @electron/get
#   的一等配置源，最可靠。同时 .npmrc 对 npm install 阶段（electron 包
#   postinstall 下载二进制）也生效，环境变量只对运行时生效。
#
# 配置项说明：
#   electron_mirror               → @electron/get 读，影响 Electron 二进制下载
#   electron_builder_binaries_mirror → electron-builder 读，影响 winCodeSign/nsis 等
#
# 镜像地址两套等价（任选其一）：
#   https://registry.npmmirror.com/-/binary/electron/        （新路径）
#   https://npmmirror.com/mirrors/electron/                  （旧路径）
# 这里用新路径，与 workflow 里 ELECTRON_MIRROR 环境变量保持一致。
#
# 不改 registry：npm 包元数据走官方 registry 速度已够，且避免 npmmirror 同步延迟。
electron_mirror=https://registry.npmmirror.com/-/binary/electron/
electron_builder_binaries_mirror=https://registry.npmmirror.com/-/binary/electron-builder-binaries/